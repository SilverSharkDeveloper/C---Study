자료구조
	1.선형 구조 : List, Stack, Queue
	2.비선형 구조 : Tree, Heap, graph

<구조체에서 포인터를 사용하는 경우>
	-자기 참조
	-외부 참조

배열의 단점(ArrayList) : 1.같은 데이터타입만 쓸 수 있다.
 			     2.배열에 선언시 배열의 크기가 결정되고 바꿀 수 없다.

->이를보완하기 위해 나온것이 LinkedList이다. ->주소값으로 접근.